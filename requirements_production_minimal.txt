# FAME Production Requirements - MINIMAL (for t3.micro EC2)
# Stripped down version that fits in 1 GB RAM
# NOTE: This removes AGI capabilities (transformers, sentence-transformers, etc.)
# For full AGI features, use t3.large+ with requirements_production.txt

# Core API & Web Server
fastapi>=0.110.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# HTTP & Networking
requests>=2.31.0
aiohttp>=3.9.0
python-dotenv>=1.0.0

# Basic Data Processing (lightweight)
numpy>=1.24.0
pandas>=2.0.0
pyyaml>=6.0.0

# System Monitoring
psutil>=5.9.0

# Financial Data (lightweight)
yfinance>=0.2.28
vaderSentiment>=3.3.2

# Web Scraping (basic)
beautifulsoup4>=4.12.0

# Logging
structlog>=23.0.0

# REMOVED for t3.micro compatibility:
# - transformers (too heavy - needs 4GB+ RAM)
# - sentence-transformers (depends on transformers)
# - chromadb (heavy vector DB - needs 2GB+ RAM)
# - faiss-cpu (huge compile - needs 4GB+ RAM)
# - pyarrow (large dependency)
# - accelerate (needs transformers)
# - scikit-learn (needs 2GB+ RAM)
# - lxml (large C compile)
# - ccxt (heavy crypto library)
# - torch (PyTorch - needs 4GB+ RAM minimum)
# - networkx (can be heavy)
# - opentelemetry (optional tracing - adds overhead)
# - docker (not needed in container)
# - Pillow (GUI not needed for API)
# - speechrecognition, pyttsx3 (voice not needed for API)

